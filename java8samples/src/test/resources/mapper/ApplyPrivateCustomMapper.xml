<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ApplyPrivateCustomMapper">

    <insert id="insert" parameterType="PropertyAuction">
        insert into applyPrivateLog (propertyId,empNo,phoneId,contactName,status) 
        values(#{propertyId},#{empNo},#{phoneId},#{contactName},#{status})
    </insert>

</mapper>